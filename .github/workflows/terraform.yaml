name: "Terraform linting"
description: "Sanitize Terraform files"

on:
  push:
    branches-ignore: "main"

jobs:
  lint:
    uses: mattias-fjellstrom-org/github-terraform-landing-zones/.github/workflows/lint.yaml@main